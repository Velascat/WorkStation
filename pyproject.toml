[project]
name = "workstation"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pyyaml>=6.0",
    "httpx>=0.27.0",
]

[tool.pytest.ini_options]
testpaths = ["test"]
pythonpath = ["tools"]
